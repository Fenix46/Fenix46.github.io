---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_EMAIL, SOCIAL_LINKS } from '../consts';
---

<!doctype html>
<html lang="it">
	<head>
		<BaseHead title={`Contatti - ${SITE_TITLE}`} description="Contatta Emanuele Scarlata per collaborazioni e opportunità" />
	</head>
	<body>
		<Header />
		<main>
			<article class="contact-content">
				<header class="contact-header">
					<h1>Contattami</h1>
					<p class="contact-subtitle">Sono sempre interessato a nuove opportunità e collaborazioni</p>
				</header>

				<section class="contact-section">
					<div class="contact-info">
						<div class="contact-card">
							<div class="contact-icon">📧</div>
							<h3>Email</h3>
							<p>Il modo migliore per raggiungermi</p>
							<a href={`mailto:${SITE_EMAIL}`} class="contact-link">
								{SITE_EMAIL}
							</a>
						</div>

						<div class="contact-card">
							<div class="contact-icon">💼</div>
							<h3>LinkedIn</h3>
							<p>Per opportunità professionali</p>
							<a href={SOCIAL_LINKS.linkedin} class="contact-link" target="_blank" rel="noopener noreferrer">
								Profilo LinkedIn
							</a>
						</div>

						<div class="contact-card">
							<div class="contact-icon">🐙</div>
							<h3>GitHub</h3>
							<p>Per vedere i miei progetti</p>
							<a href={SOCIAL_LINKS.github} class="contact-link" target="_blank" rel="noopener noreferrer">
								Profilo GitHub
							</a>
						</div>
					</div>
				</section>

				<section class="contact-section">
					<h2>Come Posso Aiutarti?</h2>
					<div class="services-grid">
						<div class="service-item">
							<h3>🚀 Sviluppo Web</h3>
							<p>Applicazioni web moderne e responsive, sia frontend che backend. 
							Specializzato in React, Node.js e architetture cloud-native.</p>
						</div>
						<div class="service-item">
							<h3>🔧 Consulenza Tecnica</h3>
							<p>Consulenza su architetture software, best practices e ottimizzazioni. 
							Aiuto a scegliere le tecnologie più adatte per i tuoi progetti.</p>
						</div>
						<div class="service-item">
							<h3>🤝 Collaborazioni</h3>
							<p>Collaborazioni su progetti open source, hackathon o iniziative 
							innovative. Sempre interessato a nuove sfide tecnologiche.</p>
						</div>
					</div>
				</section>

				<section class="contact-section">
					<h2>Risposta Garantita</h2>
					<p>
						Rispondo sempre alle email entro 24-48 ore. Se hai un progetto interessante, 
						un'opportunità di collaborazione o semplicemente vuoi fare due chiacchiere 
						sulla tecnologia, non esitare a scrivermi!
					</p>
					<div class="cta-button">
						<a href={`mailto:${SITE_EMAIL}?subject=Contatto da Portfolio`} class="btn btn-primary">
							Invia Email
						</a>
					</div>
				</section>
			</article>
		</main>
		<Footer />
	</body>
</html>

<style>
	.contact-content {
		max-width: 800px;
		margin: 0 auto;
	}

	.contact-header {
		text-align: center;
		margin-bottom: 4rem;
		padding: 2rem 0;
	}

	.contact-header h1 {
		font-size: 3rem;
		margin-bottom: 1rem;
		background: linear-gradient(135deg, var(--accent), var(--accent-dark));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.contact-subtitle {
		font-size: 1.3rem;
		color: rgb(var(--gray));
		font-weight: 500;
	}

	.contact-section {
		margin-bottom: 4rem;
	}

	.contact-section h2 {
		font-size: 2rem;
		margin-bottom: 2rem;
		color: rgb(var(--black));
		text-align: center;
	}

	/* Contact Info Cards */
	.contact-info {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-bottom: 3rem;
	}

	.contact-card {
		background: white;
		padding: 2rem;
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		border: 1px solid rgb(var(--gray-light));
		text-align: center;
		transition: transform 0.3s ease;
	}

	.contact-card:hover {
		transform: translateY(-5px);
	}

	.contact-icon {
		font-size: 3rem;
		margin-bottom: 1rem;
	}

	.contact-card h3 {
		font-size: 1.3rem;
		margin-bottom: 0.5rem;
		color: rgb(var(--black));
	}

	.contact-card p {
		color: rgb(var(--gray));
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.contact-link {
		color: var(--accent);
		text-decoration: none;
		font-weight: 600;
		font-size: 1.1rem;
		transition: color 0.3s ease;
	}

	.contact-link:hover {
		color: var(--accent-dark);
		text-decoration: underline;
	}

	/* Services Grid */
	.services-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 2rem;
		margin-top: 2rem;
	}

	.service-item {
		background: white;
		padding: 2rem;
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		border: 1px solid rgb(var(--gray-light));
		transition: transform 0.3s ease;
	}

	.service-item:hover {
		transform: translateY(-3px);
	}

	.service-item h3 {
		font-size: 1.3rem;
		margin-bottom: 1rem;
		color: var(--accent);
	}

	.service-item p {
		color: rgb(var(--gray));
		line-height: 1.6;
	}

	/* CTA Section */
	.contact-section:last-child {
		text-align: center;
		background: white;
		padding: 3rem 2rem;
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		border: 1px solid rgb(var(--gray-light));
	}

	.contact-section:last-child p {
		font-size: 1.1rem;
		color: rgb(var(--gray));
		margin-bottom: 2rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
		line-height: 1.7;
	}

	.cta-button {
		margin-top: 2rem;
	}

	.btn {
		display: inline-block;
		padding: 1rem 2rem;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
		border: 2px solid transparent;
		font-size: 1.1rem;
	}

	.btn-primary {
		background: var(--accent);
		color: white;
	}

	.btn-primary:hover {
		background: var(--accent-dark);
		transform: translateY(-2px);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.contact-header h1 {
			font-size: 2.5rem;
		}

		.contact-info {
			grid-template-columns: 1fr;
		}

		.services-grid {
			grid-template-columns: 1fr;
		}

		.contact-section h2 {
			font-size: 1.8rem;
		}
	}
</style> 