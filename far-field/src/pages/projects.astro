---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE } from '../consts';

// Dati dei progetti (in un progetto reale questi potrebbero venire da un CMS o database)
const projects = [
	{
		id: 1,
		title: 'PolyAgents',
		description: 'Sistema multi agente (cloud) con consenso semantico (con agente locale)',
		image: '🤖',
		tech: ['Python', 'PostgreSQL', 'Redis', 'Qdrant', 'React', 'TypeScript', 'Vite'],
		category: 'ai',
		github: 'https://github.com/Fenix46/polyagents',
		demo: null,
		featured: true,
		opensource: true
	},
	{
		id: 2,
		title: 'VibeCli',
		description: 'Agente di sviluppo codice direttamente da CLI',
		image: '⚡',
		tech: ['Python'],
		category: 'ai',
		github: 'https://github.com/Fenix46/vibecli',
		demo: null,
		featured: true,
		opensource: true,
		comingSoon: true
	},
	{
		id: 3,
		title: 'ElCanfin',
		description: 'Sito Web per Gruppo Folkoristico locale',
		image: '🎭',
		tech: ['PHP', 'HTML', 'CSS'],
		category: 'web',
		github: null,
		site: 'https://efcanfin.it',
		featured: false,
		opensource: false
	},
	{
		id: 4,
		title: 'LuckyKernel',
		description: 'Sviluppo kernel android device: samsung galaxy nexus',
		image: '📱',
		tech: ['C', 'Linux', 'Android', 'Kernel Development'],
		category: 'mobile',
		github: 'https://github.com/Fenix46/luckykernel',
		demo: null,
		featured: false,
		opensource: true,
		legacy: true
	}
];

const categories = [
	{ id: 'all', name: 'Tutti' },
	{ id: 'ai', name: 'AI/ML' },
	{ id: 'web', name: 'Web' },
	{ id: 'mobile', name: 'Mobile' }
];
---

<!doctype html>
<html lang="it">
	<head>
		<BaseHead title={`Progetti - ${SITE_TITLE}`} description="Portfolio dei progetti di Emanuele Scarlata - Tech Enthusiast" />
	</head>
	<body>
		<Header />
		<main>
			<header class="projects-header">
				<h1>I Miei Progetti</h1>
				<p>Una selezione dei progetti che ho realizzato, dai sistemi AI ai kernel Android.</p>
			</header>

			<section class="projects-filters">
				<div class="filter-buttons">
					{categories.map(category => (
						<button class="filter-btn" data-category={category.id}>
							{category.name}
						</button>
					))}
				</div>
			</section>

			<section class="projects-grid">
				{projects.map(project => (
					<article class="project-card" data-category={project.category}>
						<div class="project-image">
							<div class="project-placeholder">{project.image}</div>
							{project.featured && <span class="featured-badge">⭐ In Evidenza</span>}
							{project.opensource && <span class="opensource-badge">🔓 Open Source</span>}
							{project.comingSoon && <span class="coming-soon-badge">🚧 Coming Soon</span>}
							{project.legacy && <span class="legacy-badge">📜 Legacy</span>}
						</div>
						<div class="project-content">
							<h3>{project.title}</h3>
							<p>{project.description}</p>
							<div class="project-tech">
								{project.tech.map(tech => <span class="tech-tag">{tech}</span>)}
							</div>
							<div class="project-links">
								{project.site && <a href={project.site} class="project-link" target="_blank" rel="noopener noreferrer">Sito</a>}
								{project.github && <a href={project.github} class="project-link" target="_blank" rel="noopener noreferrer">GitHub</a>}
							</div>
						</div>
					</article>
				))}
			</section>

			<section class="projects-cta">
				<h2>Hai un progetto in mente?</h2>
				<p>Sono sempre interessato a nuove opportunità e collaborazioni. Se hai un'idea o un progetto che vorresti discutere, non esitare a contattarmi!</p>
				<a href="/contact" class="btn btn-primary">Contattami</a>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.projects-header {
		text-align: center;
		margin-bottom: 4rem;
		padding: 2rem 0;
	}

	.projects-header h1 {
		font-size: 3rem;
		margin-bottom: 1rem;
		background: linear-gradient(135deg, var(--accent), var(--accent-dark));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.projects-header p {
		font-size: 1.2rem;
		color: rgb(var(--gray));
		max-width: 600px;
		margin: 0 auto;
	}

	/* Filters */
	.projects-filters {
		margin-bottom: 3rem;
	}

	.filter-buttons {
		display: flex;
		justify-content: center;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.filter-btn {
		padding: 0.75rem 1.5rem;
		border: 2px solid rgb(var(--gray-light));
		background: white;
		color: rgb(var(--gray-dark));
		border-radius: 25px;
		cursor: pointer;
		transition: all 0.3s ease;
		font-weight: 500;
	}

	.filter-btn:hover,
	.filter-btn.active {
		background: var(--accent);
		color: white;
		border-color: var(--accent);
	}

	/* Projects Grid */
	.projects-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
		gap: 2rem;
		margin-bottom: 4rem;
	}

	.project-card {
		background: white;
		border-radius: 12px;
		overflow: hidden;
		box-shadow: var(--box-shadow);
		border: 1px solid rgb(var(--gray-light));
		transition: all 0.3s ease;
		position: relative;
	}

	.project-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 8px 32px rgba(var(--gray), 40%);
	}

	.project-image {
		height: 200px;
		background: linear-gradient(135deg, rgb(var(--gray-light)), white);
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
	}

	.project-placeholder {
		font-size: 4rem;
	}

	.featured-badge {
		position: absolute;
		top: 1rem;
		right: 1rem;
		background: var(--accent);
		color: white;
		padding: 0.3rem 0.8rem;
		border-radius: 15px;
		font-size: 0.8rem;
		font-weight: 600;
	}

	.opensource-badge {
		position: absolute;
		top: 1rem;
		left: 1rem;
		background: #28a745;
		color: white;
		padding: 0.3rem 0.8rem;
		border-radius: 15px;
		font-size: 0.8rem;
		font-weight: 600;
	}

	.coming-soon-badge {
		position: absolute;
		bottom: 1rem;
		left: 1rem;
		background: #ffc107;
		color: #212529;
		padding: 0.3rem 0.8rem;
		border-radius: 15px;
		font-size: 0.8rem;
		font-weight: 600;
	}

	.legacy-badge {
		position: absolute;
		bottom: 1rem;
		right: 1rem;
		background: #6c757d;
		color: white;
		padding: 0.3rem 0.8rem;
		border-radius: 15px;
		font-size: 0.8rem;
		font-weight: 600;
	}

	.project-content {
		padding: 1.5rem;
	}

	.project-content h3 {
		margin-bottom: 0.5rem;
		color: rgb(var(--black));
		font-size: 1.3rem;
	}

	.project-content p {
		color: rgb(var(--gray));
		margin-bottom: 1rem;
		line-height: 1.6;
	}

	.project-tech {
		display: flex;
		flex-wrap: wrap;
		gap: 0.5rem;
		margin-bottom: 1rem;
	}

	.tech-tag {
		background: var(--accent);
		color: white;
		padding: 0.3rem 0.6rem;
		border-radius: 15px;
		font-size: 0.8rem;
		font-weight: 500;
	}

	.project-links {
		display: flex;
		gap: 1rem;
	}

	.project-link {
		color: var(--accent);
		text-decoration: none;
		font-weight: 500;
		font-size: 0.9rem;
		padding: 0.5rem 1rem;
		border: 1px solid var(--accent);
		border-radius: 6px;
		transition: all 0.3s ease;
	}

	.project-link:hover {
		background: var(--accent);
		color: white;
	}

	/* CTA Section */
	.projects-cta {
		text-align: center;
		background: white;
		padding: 3rem 2rem;
		border-radius: 12px;
		box-shadow: var(--box-shadow);
		border: 1px solid rgb(var(--gray-light));
	}

	.projects-cta h2 {
		font-size: 2rem;
		margin-bottom: 1rem;
		color: rgb(var(--black));
	}

	.projects-cta p {
		font-size: 1.1rem;
		color: rgb(var(--gray));
		margin-bottom: 2rem;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.btn {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
		border: 2px solid transparent;
	}

	.btn-primary {
		background: var(--accent);
		color: white;
	}

	.btn-primary:hover {
		background: var(--accent-dark);
		transform: translateY(-2px);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.projects-header h1 {
			font-size: 2.5rem;
		}

		.projects-grid {
			grid-template-columns: 1fr;
		}

		.filter-buttons {
			gap: 0.5rem;
		}

		.filter-btn {
			padding: 0.5rem 1rem;
			font-size: 0.9rem;
		}
	}
</style>

<script>
	// Filtro progetti
	document.addEventListener('DOMContentLoaded', function() {
		const filterButtons = document.querySelectorAll('.filter-btn');
		const projectCards = document.querySelectorAll('.project-card');

		filterButtons.forEach(button => {
			button.addEventListener('click', function() {
				const category = this.getAttribute('data-category');
				
				// Aggiorna stato attivo dei bottoni
				filterButtons.forEach(btn => btn.classList.remove('active'));
				this.classList.add('active');

				// Filtra i progetti
				projectCards.forEach(card => {
					const cardCategory = card.getAttribute('data-category');
					if (category === 'all' || cardCategory === category) {
						card.style.display = 'block';
					} else {
						card.style.display = 'none';
					}
				});
			});
		});

		// Imposta "Tutti" come attivo di default
		document.querySelector('[data-category="all"]').classList.add('active');
	});
</script> 